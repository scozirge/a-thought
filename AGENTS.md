# AThought 專案規範

AThought 是 scozirge 的大雜燴專案，用不同資料夾保存各類想法與子專案。

- `musings/`：與 AI 的隨想、對談及討論紀錄。處理這類出題、延續討論、整理或存檔工作前，先閱讀並遵守 `musings/AGENTS.md`。
- `joylings/`：獨立的子專案資料夾，依該資料夾內的規範與使用者當次要求處理。
- 其他子專案若有自己的 `AGENTS.md`，處理該子專案前先閱讀；各子專案的專用規範僅適用於對應範圍。
- 預設使用繁體中文，保留使用者既有修改，只調整當次工作需要的內容。
