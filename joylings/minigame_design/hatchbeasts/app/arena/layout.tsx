import type { Metadata } from 'next';

export const metadata: Metadata = { title: 'RIVALS｜H5 單機原型' };

export default function ArenaLayout({ children }: { children: React.ReactNode }) {
  return children;
}
