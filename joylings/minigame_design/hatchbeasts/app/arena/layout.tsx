import type { Metadata } from 'next';

export const metadata: Metadata = { title: '方塊泡泡戰｜單機 3D 範例' };

export default function ArenaLayout({ children }: { children: React.ReactNode }) {
  return children;
}
